// Dependencies
import React from "react";
// Custom
import Layout from "../layouts";

const Index = (props) => {
  const { posts } = props;

  return (
    <Layout>
      TESTOWY!
    </Layout>
  );
}

export default Index;
